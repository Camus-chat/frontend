'use client';

import { Button } from '@/shared/ui';

const EnterAsGuest = () => {
  return (
    <Button variant='light' className='w-full'>
      게스트로 참여하기
    </Button>
  );
};

export default EnterAsGuest;
